# [수업 자료] Flutter로 만드는 효도앱: 시니어 QR 리더기

## 🎯 1. 수업 목표 (Learning Objectives)
이 프로젝트를 통해 학생들은 다음 핵심 역량을 기를 수 있습니다:
1.  **실전 패키지 활용**: 카메라(`mobile_scanner`), 권한(`permission_handler`), 저장소(`shared_preferences`) 등 필수 라이브러리 사용법 습득.
2.  **하드웨어 제어**: 스마트폰의 **카메라(영상)**, **플래시(조명)**, **진동(피드백)**을 코드로 제어하는 경험.
3.  **사용자 중심(UX) 설계**: "누구를 위한 앱인가?"를 고민하며, 대상 사용자(시니어)에 맞춰 기능을 단순화하고 UI를 큼직하게 구성하는 설계 능력.
4.  **비동기 프로그래밍**: `Future`, `async/await`를 활용한 데이터 처리 및 저장 로직 이해.

---

## 🛠️ 2. 준비물 및 사전 지식
*   **개발 환경**: VS Code, Flutter SDK 설치 완료
*   **사전 지식**: Dart 언어 기초 (변수, 함수, 클래스), Flutter 기본 위젯 (Scaffold, Container, Row/Column)

---

## 📚 3. 단계별 실습 가이드 (Step-by-Step)

### [1단계] 프로젝트 세팅과 '뼈대' 만들기
가장 먼저 앱의 기초를 다집니다. `pubspec.yaml`에 필요한 도구들을 정의합니다.
*   **핵심 질문**: "우리가 만들 앱에 필요한 기능은 무엇인가?" (카메라, 진동, 인터넷 연결 등)
*   **실습**: `mobile_scanner`, `url_launcher`, `shared_preferences` 의존성 추가하기.

### [2단계] "카메라야 켜져라!" - 권한과 스캐너
앱이 켜지자마자 카메라가 실행되도록 만듭니다.
*   **핵심 개념**: 사용자의 '허락(Permission)'을 받아야만 카메라를 쓸 수 있다는 개인정보보호 개념 설명.
*   **실습**: `permission_handler`로 권한 요청 로직 짜기, `MobileScanner` 위젯 배치하기.

### [3단계] "이게 뭐야?" - 데이터 분석하기 (Parsing)
QR코드는 단순한 문자열입니다. 이것이 '웹사이트 주소'인지, 단순 '글자'인지 구별해야 합니다.
*   **핵심 개념**: 문자열 처리 (String parsing).
*   **실습**: `http://` 또는 `https://`로 시작하면 URL로 판단하는 `QrParser` 클래스 만들기.

### [4단계] "잘 보여요!" - 시니어 맞춤형 결과 화면
어르신들은 작은 글씨를 힘들어하십니다.
*   **UX 포인트**: 최소 24pt 이상의 폰트, 검정 배경에 노란색/흰색 글씨 (고대비).
*   **실습**: `ResultScreen` 구현. 복잡한 아이콘 대신 "사이트 연결하기" 같은 명확한 한글 버튼 만들기.

### [5단계] "지워지지 않는 기억" - 데이터 저장 (Persistence)
앱을 껐다 켜도 기록이 남아있도록 만듭니다.
*   **핵심 개념**: 로컬 데이터베이스와 JSON 직렬화.
*   **실습**: `ScanRecord` 모델 클래스(`toJson`/`fromJson`) 만들기, `Shared Preferences`에 리스트 저장하기.

### [6단계] 디테일의 차이 - 플래시와 진동
사용성을 높이는 '한 끗 차이'를 만듭니다.
*   **실습**: 스캔 성공 시 `Vibration.vibrate()` 호출, 어두운 곳을 위한 플래시 토글 버튼 구현.

---

## 💡 4. 심화 과제 (Challenge)
수업 후 더 나아가고 싶은 학생들을 위한 과제입니다.
1.  **공유하기 기능**: 스캔한 내용을 카카오톡이나 문자로 가족에게 보내는 기능 추가해보기 (`share_plus` 패키지).
2.  **QR 코드 생성**: 읽기뿐만 아니라, 내 연락처를 QR코드로 만들어 보여주는 기능 추가해보기.
3.  **소리 읽어주기**: 시력이 안 좋은 분들을 위해 스캔 결과를 음성으로 읽어주는 기능(TTS) 추가해보기.

---

## 📝 5. 수업 팁 (Instructor Tips)
*   **웹(Web) vs 앱(App)**: 웹 브라우저에서 실행할 때는 카메라 권한이나 진동 기능에 제약이 있음을 시연하며 플랫폼 차이를 설명해주세요.
*   **에러 대처**: "빨간 화면(에러)"이 떴을 때 당황하지 않고 로그(`Debug Console`)를 읽는 법을 지도해주세요.
*   **칭찬 포인트**: 학생들이 복잡한 기능을 구현했을 때보다, **"사용자를 배려한 디자인"**을 했을 때 더 크게 칭찬해주세요. 기술보다 '마음'이 담긴 기술이 중요함을 가르쳐주세요.
